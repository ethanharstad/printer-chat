{
  "name": "printer-chat",
  "version": "0.0.1",
  "description": "Generative AI chatbot with general knowledge about 3d printers",
  "main": "index.js",
  "scripts": {
    "dev": "wrangler pages dev ./public",
    "deploy": "wrangler pages deploy ./public",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@cloudflare/ai": "^1.0.18",
    "hono": "^3.7.6"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20231010.0",
    "wrangler": "^3.12.0"
  }
}
